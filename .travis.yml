if: (type = push AND branch IN (develop, dev)) OR (type = pull_request AND NOT branch =~ /no-ci/)
language: node_js
node_js:
  - 'stable'
cache:
  directories:
    - 'node_modules'

before_script:
  - npm install  
  
script: 
  - npm run test
after_success:
- npm run coveralls
